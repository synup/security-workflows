"""Synup modular code scanner.

Each check lives in its own module under `scan/checks/` and can be toggled via
`.synup-scan.json` or `--disable`. See `runner.py` for the CLI entry point.
"""
__version__ = "2.0.0"
